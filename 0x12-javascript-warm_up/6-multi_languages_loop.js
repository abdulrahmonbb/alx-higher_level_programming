#!/usr/bin/node
// Prints 3 lines using an array and a for loop

const languages = ['C is fun', 'Python is cool', 'Javascript is amazing'];

for (let i = 0; i < 3; i++) {
  console.log(languages[i]);
}
